import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  resultsContent: {
    gap: 18,
  },

  content: {
    flex: 1,
    justifyContent: 'center',
    alignSelf: 'center',
  },

  noResultsText: {
    fontSize: 22,
    textAlign: 'center',
  },

  image: {
    justifyContent: 'center',
    alignSelf: 'center',
  },
});
